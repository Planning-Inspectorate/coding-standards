import type { UserConfig as CommitlintConfig } from '@commitlint/types';
import type { ConfigWithExtendsArray as EslintConfig } from '@eslint/config-helpers';
import type { Config as PrettierConfig } from 'prettier';
import type { Config as StylelintConfig } from 'stylelint';

export declare const commitlintConfig: CommitlintConfig;
export declare const eslintConfig: EslintConfig;
export declare const prettierConfig: PrettierConfig;
export declare const stylelintConfig: StylelintConfig;
